import { db } from './lib/db'; import { documents } from './lib/db/schema'; async function run() { try { await db.insert(documents).values({ id: 'test-1234', userId: '09c3b589-91ae-47f4-a314-2cc30fc09f14', type: 'faktur', createdAt: Date.now(), updatedAt: Date.now(), docNumber: 'FKT-TEST', date: '2026-08-01' }); console.log('SUCCESS'); } catch (e) { console.error('ERROR', e); } } run();